name: Build and Deploy mysql Service

on:
  push:
    branches:
      - main
    paths:
      - 'mysql/**'
      - 'Helmcharts/mysql/**'
  workflow_dispatch:

jobs:
  build:
    name: Build and Push mysql Image
    runs-on: ubuntu-latest
    permissions:  
      id-token: write   # required for OIDC
      contents: read    # allow reading repo content

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v2
      - name: Verify mysql folder exists
        run: |
          if [ ! -d "./mysql" ]; then
            echo "❌ ERROR: ./mysql folder not found. Check your repo structure."
            ls -R
            exit 1
          else
            echo "✅ ./mysql folder found, proceeding..."
          fi
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 4️⃣ Build and push the image
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./mysql
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/mysql:latest

      # - name: Build and Push Image
      #   run: |
          # IMAGE=${{ steps.login-ecr.outputs.registry }}/user:latest
          # docker build -t $IMAGE ./user
          # docker push $IMAGE

  # deploy:
  #   name: Deploy Web to EKS
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v3

  #     - name: Configure AWS Credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
  #         aws-region: us-east-1

  #     - name: Update kubeconfig
  #       run: |
  #         aws eks update-kubeconfig --name my-eks-cluster --region us-east-1

  #     - name: Deploy Helm Chart
  #       run: |
  #         helm upgrade web ./EKS/helm \
  #           --install \
  #           --namespace robot-shop \
  #           --create-namespace \
  #           --set image.repo=${{ steps.login-ecr.outputs.registry }} \
  #           --set image.version=latest
